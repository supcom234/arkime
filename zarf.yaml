kind: ZarfPackageConfig
metadata:
  name: arkime
  description: "Arkime Traffic Monitoring / Analysis"
  # Big Bang / Iron Bank are only amd64
  architecture: amd64

variables:
  # Prompt user for the domain override
  - name: DOMAIN
    default: "vp.bigbang.dev"
    prompt: false

  - name: BRANCH
    default: "main"
    prompt: true

components:
  - name: arkime
    required: true
    manifests:
      - name: arkime-helm
        files:
          - manifests/namespace.yaml
          - manifests/gitrepository.yaml
          - manifests/helmrelease.yaml
          - manifests/virtualservice.yaml
    images:
      - harbor.cnaps.io/voltron/arkime3:latest